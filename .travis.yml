language: cpp
compiler:
  - gcc
  - clang
before_install:
  - if [ $TRAVIS_OS_NAME == linux ]; then sudo apt-get update && sudo apt-get install -y cmake llvm-3.4 llvm-3.4-dev; fi

script: mkdir build && cd build && cmake .. && make && ./test
